# Nexmo Webhook Router
This router is used for accepting Nexmo Webhook requests, such as:

- inbound webhook (SMS, MAPI)
- delivery webhook (SMS)
- status webhook (MAPI)
- answer webhook (VAPI)
- fallback answer webhook (VAPI)
- event webhook (VAPI)
- RTC webhook (RTC)
